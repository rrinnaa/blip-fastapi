# BLIP FastAPI

FastAPI-сервер для генерации описаний изображений с использованием модели BLIP-2 и перевода на русский язык.

## О проекте

Данный проект представляет собой FastAPI-сервер, который:
- Принимает фотографии с Android-приложения
- Обрабатывает их с помощью модели BLIP-2 для генерации краткого описания
- Переводит текст на русский язык при необходимости
- Возвращает результат обратно на устройство

### 🔗 Android-приложение
Код мобильного приложения доступен в [соответствующем репозитории](https://github.com/your-username/android-app-repo)

## Возможности

- **Генерация описаний**: Использует модель `Salesforce/blip2-flan-t5-xl` для создания подписей к изображениям
- **Фокус на видимом**: Описывает только видимые объекты без предположений о местоположении, эмоциях или действиях
- **Перевод на русский**: Использует модель `facebook/m2m100_418M` для перевода результатов
- **REST API**: Доступ через единственный endpoint `/describe`

## Предварительные требования

- Python 3.10+ (рекомендуется 3.10 или 3.11)
- Git
- Доступ в интернет (для загрузки моделей при первом запуске)
- Свободное место: ~несколько ГБ для моделей BLIP-2 и M2M100

## Рекомендуемая структура проекта

```
blip_fastapi/
├── app.py              
├── requirements.txt  
├── .gitignore       
└── README.md          
```

## Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/rrinnaa/blip-fastapi.git
cd blip-fastapi
```

### 2. Создание виртуального окружения

**Windows:**
```cmd
python -m venv .venv
```

**macOS / Linux:**
```bash
python3 -m venv .venv
```

### 3. Активация виртуального окружения

**Windows (PowerShell):**
```powershell
.venv\Scripts\Activate.ps1
```

**macOS / Linux:**
```bash
source .venv/bin/activate
```

### 4. Установка зависимостей

Создайте файл `requirements.txt`:
```txt
fastapi
uvicorn[standard]
transformers
torch
Pillow
```

Установите зависимости:
```bash
pip install -r requirements.txt
```

**Или установите без файла:**
```bash
pip install fastapi uvicorn[standard] transformers torch Pillow
```

### Дополнительные настройки

**Для GPU (CUDA):**
Установите совместимую версию PyTorch с поддержкой CUDA согласно [официальной инструкции](https://pytorch.org)

**Для Apple Silicon (M1/M2/M3/M4):**
Установите PyTorch с поддержкой MPS по [официальной инструкции](https://pytorch.org)

## 🚀 Запуск сервера

```bash
uvicorn app:app --host 0.0.0.0 --port 8000
```

## 📱 Работа с Android-приложением

После успешного запуска сервера можно:
1. Настроить Android-приложение для подключения к серверу
2. Отправлять фотографии на endpoint `/describe`
3. Получать текстовые описания с последующим озвучиванием
